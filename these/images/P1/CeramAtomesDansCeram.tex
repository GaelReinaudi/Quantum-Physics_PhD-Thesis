\documentclass[11pt, a4paper, french]{thesul}%{article}
\input{../../Packages_Gael}

%%% Pour n'avoir que l'image tikz ! ! !
\usepackage[active,tightpage]{preview}\PreviewEnvironment{tikzpicture} 

\usepackage[np,autolanguage]{numprint}
\begin{document}
\pagestyle{empty}
\input{../../New_Commande_Gael}

\begin{tikzpicture}[scale=0.9]

\def\distSurf{3}  
\def\epCer{1}
\useasboundingbox (-\distSurf-\epCer,-\distSurf-\epCer) rectangle(\distSurf+\epCer,\distSurf+\epCer);
\fill[color=black!70] (-\distSurf-\epCer,-\distSurf-\epCer) rectangle(\distSurf+\epCer,\distSurf+\epCer);
\fill[white] (0,0) circle(\distSurf);

\path(0,0)node{$\otimes$}node[above right]{ $z$};
\draw(-\distSurf+1,\distSurf)--+(\distSurf/3,-\distSurf/3)node[right]{c�ramique};

\def\numAt{20}
\def\sizeCloud{1.7}
\foreach \xx in {1,...,\numAt}{
\foreach \yy in {1,...,\numAt}{
\pgfmathparse{1*(rand)*(\sizeCloud*\xx/\numAt)-2.9}
\let\XX\pgfmathresult
\pgfmathparse{1*(rand)*(\sizeCloud*\yy/\numAt)}
\let\YY\pgfmathresult
\pgfmathparse{\XX*\XX+\YY*\YY > \distSurf*\distSurf}
\let\Elimi\pgfmathresult

\ifthenelse{1=\Elimi}
{
	\shade[ball color=black!30] (\XX,\YY) circle (0.10cm);
}
{
	\shade[ball color=blue] (\XX,\YY) circle (0.10cm);
}
}}

\draw[red,thick,latex'-latex'](0,0)--(-\distSurf,0)node[pos=0.4, below right=.1cm]
{ $\Devi=\CeramRay$};


\end{tikzpicture}

\end{document}